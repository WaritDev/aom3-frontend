import React from 'react';
import { Typography, Container } from '@mui/material';

export default function VaultPage() {
    return (
        <Container maxWidth="lg">
        <Typography variant="h4" fontWeight="bold" color="white">Active Vaults</Typography>
        </Container>
    );
}